export type UserData = {
  userId: string;
  username: string;
  profileUrl: string;
  time: string;
};

export type UserChat = {
  userId: string;
  text: string;
  profileUrl: string;
  senderName: string;
  createdAt: string;
};
